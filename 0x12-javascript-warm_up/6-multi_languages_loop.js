#!/usr/bin/node
/* prints 3 lines but by uusing an array of string and a loop */
const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let i = 0; i < languages.length; i++) {
	console.log(languages[i]);
}
