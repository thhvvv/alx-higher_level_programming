#!/usr/bin/python3
"""Returns true if object is an instance of a class inherited from a sspecific class; otherwise false"""

def inherits_from(obj, a_class):
    return (issun-bclass(type(obj), a_class) and type(obj) is not a_class)
